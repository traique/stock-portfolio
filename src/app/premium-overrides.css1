/*
  premium-overrides.css
  Component-level additions only — Liquid Glass iOS 26 refinements.
  Không override token gốc, không dùng !important.
*/

/* =========================================================
   HERO SUBLINE TAG
========================================================= */
.ab-hero-subline {
  display:     inline-flex;
  align-items: center;
  gap:         8px;
  padding:     8px 14px;
  border-radius: 999px;
  border:      1px solid;
  font-size:   12px;
  font-weight: 700;
}

/* =========================================================
   MARKET STRIP
========================================================= */
.ab-strip-item {
  display:         flex;
  flex-direction:  column;
  justify-content: center;
  padding:         7px 11px;
}

.ab-strip-item.vnindex {
  border-right:   1px solid var(--border);
  padding-right:  15px;
}

.ab-strip-stack {
  display:     flex;
  align-items: center;
}

/* =========================================================
   POSITION CARD EXTRAS
========================================================= */
.ab-position-meta {
  display:               grid;
  grid-template-columns: 1fr 1fr;
  gap:                   8px;
  margin-top:            12px;
}

.ab-position-meta-cell {
  display:        flex;
  flex-direction: column;
  gap:            3px;
  padding:        10px 12px;
  border-radius:  14px;
  background:     rgba(255,255,255,0.40);
  border-top:    1px solid rgba(255,255,255,0.82);
  border-left:   1px solid rgba(255,255,255,0.82);
  border-right:  1px solid rgba(255,255,255,0.14);
  border-bottom: 1px solid rgba(255,255,255,0.14);
}

:root[data-theme='dark'] .ab-position-meta-cell {
  background:    rgba(255,255,255,0.04);
  border-top:    1px solid rgba(255,255,255,0.09);
  border-left:   1px solid rgba(255,255,255,0.09);
  border-right:  1px solid rgba(0,0,0,0.22);
  border-bottom: 1px solid rgba(0,0,0,0.22);
}

.ab-position-meta-label {
  font-size:      10px;
  font-weight:    800;
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color:          var(--subtle);
}

.ab-position-meta-value {
  font-size:   15px;
  font-weight: 800;
  color:       var(--text);
}

/* =========================================================
   AI SECTION
========================================================= */
.ab-ai-action-card {
  padding:        13px;
  border-radius:  16px;
  background:     var(--card);
  backdrop-filter: var(--glass-backdrop);
  -webkit-backdrop-filter: var(--glass-backdrop);
  border-top:    1px solid var(--glass-ring);
  border-left:   1px solid var(--glass-ring);
  border-right:  1px solid var(--glass-ring-b);
  border-bottom: 1px solid var(--glass-ring-b);
  display:        flex;
  flex-direction: column;
  gap:            10px;
}

.ab-ai-tp-sl {
  display:               grid;
  grid-template-columns: 1fr 1fr;
  gap:        10px;
  text-align: center;
  font-size:  13px;
  font-weight: 800;
}

.ab-ai-tp {
  background: var(--green-surface);
  border: 1px solid var(--green-border);
  padding:       8px 0;
  border-radius: 12px;
}

.ab-ai-sl {
  background: var(--red-surface);
  border: 1px solid var(--red-border);
  padding:       8px 0;
  border-radius: 12px;
}

.ab-ai-label {
  font-size:      10px;
  letter-spacing: 0.04em;
  font-weight:    800;
  text-transform: uppercase;
}

.ab-ai-value  { margin-top: 4px; font-size: 16px; }

.ab-ai-summary {
  padding:        14px;
  background:     rgba(255,255,255,0.38);
  border-radius:  14px;
  font-style:     italic;
  border-top:    1px solid rgba(255,255,255,0.78);
  border-left:   1px solid rgba(255,255,255,0.78);
  border-right:  1px solid rgba(255,255,255,0.12);
  border-bottom: 1px solid rgba(255,255,255,0.12);
  font-size:  14px;
  line-height: 1.55;
  color:       var(--text);
}

:root[data-theme='dark'] .ab-ai-summary {
  background:    rgba(255,255,255,0.04);
  border-top:    1px solid rgba(255,255,255,0.09);
  border-left:   1px solid rgba(255,255,255,0.09);
  border-right:  1px solid rgba(0,0,0,0.22);
  border-bottom: 1px solid rgba(0,0,0,0.22);
}

.ab-ai-risks {
  padding:       14px;
  background:    rgba(244,63,94,0.05);
  border-radius: 14px;
  border:        1px solid rgba(244,63,94,0.18);
}

.ab-ai-risks-label { color: var(--red); font-weight: 800; font-size: 13px; }
.ab-ai-risks-text  { font-weight: 600; font-size: 13px; }

/* =========================================================
   RISK PROFILE SELECTOR
========================================================= */
.ab-risk-selector { display: flex; gap: 6px; flex-wrap: wrap; }

.ab-risk-btn {
  padding:        6px 14px;
  border-radius:  999px;
  background:     rgba(255,255,255,0.42);
  border-top:    1px solid rgba(255,255,255,0.85);
  border-left:   1px solid rgba(255,255,255,0.85);
  border-right:  1px solid rgba(255,255,255,0.18);
  border-bottom: 1px solid rgba(255,255,255,0.18);
  color:          var(--subtle);
  font-size:      11px;
  font-weight:    800;
  letter-spacing: 0.04em;
  text-transform: uppercase;
  cursor:         pointer;
  transition:     all 0.18s ease;
}

:root[data-theme='dark'] .ab-risk-btn {
  background:    rgba(255,255,255,0.08);
  border:        1px solid rgba(255,255,255,0.16);
  color:         rgba(255,255,255,0.55);
}

.ab-risk-btn:hover { color: var(--text); box-shadow: var(--shadow-soft); }

.ab-risk-btn.active {
  background:   #3b82f6;
  color:        #fff;
  border-color: #3b82f6;
  box-shadow:   0 2px 8px rgba(59,130,246,0.35);
}

:root[data-theme='dark'] .ab-risk-btn.active {
  background:   #2563eb;
  border-color: #2563eb;
  box-shadow:   0 2px 12px rgba(37,99,235,0.45);
}

/* =========================================================
   NEWS MODAL
========================================================= */
.ab-modal-overlay {
  position:        fixed;
  inset:           0;
  background:      rgba(0,0,0,0.45);
  backdrop-filter: blur(12px) saturate(160%);
  -webkit-backdrop-filter: blur(12px) saturate(160%);
  z-index:         1000;
  display:         flex;
  align-items:     center;
  justify-content: center;
  padding:         16px;
}

.ab-modal-inner {
  width:      100%;
  max-width:  450px;
  max-height: 80vh;
  overflow-y: auto;
  position:   relative;
  padding:    20px;
  /* modal glass */
  background:  rgba(255,255,255,0.68);
  backdrop-filter: blur(32px) saturate(180%);
  -webkit-backdrop-filter: blur(32px) saturate(180%);
  border-top:    1px solid rgba(255,255,255,0.90);
  border-left:   1px solid rgba(255,255,255,0.90);
  border-right:  1px solid rgba(255,255,255,0.22);
  border-bottom: 1px solid rgba(255,255,255,0.22);
  border-radius: 28px;
  box-shadow: 0 24px 60px rgba(99,120,180,0.22);
}

:root[data-theme='dark'] .ab-modal-inner {
  background:    rgba(12,20,40,0.75);
  border-top:    1px solid rgba(255,255,255,0.14);
  border-left:   1px solid rgba(255,255,255,0.14);
  border-right:  1px solid rgba(0,0,0,0.28);
  border-bottom: 1px solid rgba(0,0,0,0.28);
  box-shadow: 0 24px 60px rgba(0,0,0,0.55);
}

.ab-news-item {
  display:       block;
  padding:       13px;
  background:    rgba(255,255,255,0.50);
  border-radius: 16px;
  border-top:    1px solid rgba(255,255,255,0.88);
  border-left:   1px solid rgba(255,255,255,0.88);
  border-right:  1px solid rgba(255,255,255,0.14);
  border-bottom: 1px solid rgba(255,255,255,0.14);
  text-decoration: none;
  transition:    box-shadow 0.18s ease, transform 0.18s ease;
}

:root[data-theme='dark'] .ab-news-item {
  background:    rgba(255,255,255,0.05);
  border-top:    1px solid rgba(255,255,255,0.10);
  border-left:   1px solid rgba(255,255,255,0.10);
  border-right:  1px solid rgba(0,0,0,0.22);
  border-bottom: 1px solid rgba(0,0,0,0.22);
}

.ab-news-item:hover {
  box-shadow: var(--shadow-soft);
  transform:  translateY(-1px);
}

.ab-news-title {
  color:       var(--text);
  font-weight: 700;
  font-size:   14px;
  margin-bottom: 8px;
  line-height: 1.4;
}

.ab-news-meta {
  font-size:      11px;
  font-weight:    700;
  color:          var(--subtle);
  text-transform: uppercase;
}

/* =========================================================
   SPIN ANIMATION
========================================================= */
@keyframes spin {
  from { transform: rotate(0deg); }
  to   { transform: rotate(360deg); }
}
.spin-animation { animation: spin 0.8s linear infinite; }

/* =========================================================
   COMPACT FORM GRID
========================================================= */
.compact-form-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(175px, 1fr));
  gap: 10px;
}

/* =========================================================
   TOGGLE ROW
========================================================= */
.ab-toggle-row {
  display:     flex;
  align-items: center;
  gap:         10px;
  cursor:      pointer;
}

.ab-toggle-row input[type='checkbox'] {
  width:        16px;
  height:       16px;
  accent-color: var(--text);
  cursor:       pointer;
}

/* =========================================================
   PNL BADGE — Liquid Glass glow pill
========================================================= */
.lcta-pnl-pos {
  background:    rgba(16,185,129,0.12);
  border-top:    1px solid rgba(16,185,129,0.40);
  border-left:   1px solid rgba(16,185,129,0.40);
  border-right:  1px solid rgba(16,185,129,0.15);
  border-bottom: 1px solid rgba(16,185,129,0.15);
  box-shadow:    0 0 16px rgba(16,185,129,0.16);
  color:         var(--green);
  border-radius: 16px;
  padding:       11px 14px;
}

.lcta-pnl-neg {
  background: var(--red-surface);
  border-top:    1px solid rgba(244,63,94,0.36);
  border-left:   1px solid rgba(244,63,94,0.36);
  border-right:  1px solid rgba(244,63,94,0.12);
  border-bottom: 1px solid rgba(244,63,94,0.12);
  box-shadow:    0 0 16px rgba(244,63,94,0.14);
  color:         var(--red);
  border-radius: 16px;
  padding:       11px 14px;
}

/* =========================================================
   MINI STAT CELL — bento inner cell
========================================================= */
.lcta-mini-stat {
  border-radius: 15px;
  padding:       11px 12px;
  background:    rgba(255,255,255,0.48);
  border-top:    1px solid rgba(255,255,255,0.88);
  border-left:   1px solid rgba(255,255,255,0.88);
  border-right:  1px solid rgba(255,255,255,0.14);
  border-bottom: 1px solid rgba(255,255,255,0.14);
  display:       flex;
  flex-direction: column;
  gap:           4px;
}

:root[data-theme='dark'] .lcta-mini-stat {
  background:    rgba(255,255,255,0.05);
  border-top:    1px solid rgba(255,255,255,0.10);
  border-left:   1px solid rgba(255,255,255,0.10);
  border-right:  1px solid rgba(0,0,0,0.22);
  border-bottom: 1px solid rgba(0,0,0,0.22);
}

.lcta-mini-stat-label {
  font-size:      10px;
  font-weight:    800;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color:          var(--subtle);
}

.lcta-mini-stat-value {
  font-size:   15px;
  font-weight: 800;
  color:       var(--text);
  line-height: 1.2;
}

/* =========================================================
   GLASS PILL — reusable inline pill
========================================================= */
.lcta-pill {
  display:        inline-flex;
  align-items:    center;
  gap:            5px;
  border-radius:  999px;
  padding:        5px 12px;
  background:     rgba(255,255,255,0.52);
  border-top:    1px solid rgba(255,255,255,0.88);
  border-left:   1px solid rgba(255,255,255,0.88);
  border-right:  1px solid rgba(255,255,255,0.18);
  border-bottom: 1px solid rgba(255,255,255,0.18);
  font-size:      11px;
  font-weight:    800;
  letter-spacing: 0.04em;
  color:          var(--text);
  cursor:         default;
  white-space:    nowrap;
  transition:     box-shadow 0.18s ease;
}

:root[data-theme='dark'] .lcta-pill {
  background:    rgba(255,255,255,0.08);
  border-top:    1px solid rgba(255,255,255,0.14);
  border-left:   1px solid rgba(255,255,255,0.14);
  border-right:  1px solid rgba(0,0,0,0.22);
  border-bottom: 1px solid rgba(0,0,0,0.22);
}

.lcta-pill-btn {
  cursor: pointer;
}

.lcta-pill-btn:hover { box-shadow: var(--shadow-soft); }

/* =========================================================
   ALLOCATION BAR
========================================================= */
.lcta-alloc-track {
  width:        100%;
  height:       6px;
  border-radius: 999px;
  background:   rgba(255,255,255,0.38);
  border-top:   1px solid rgba(255,255,255,0.65);
  overflow:     hidden;
}

:root[data-theme='dark'] .lcta-alloc-track {
  background: rgba(255,255,255,0.08);
  border-top: 1px solid rgba(255,255,255,0.12);
}

.lcta-alloc-fill {
  height:        100%;
  border-radius: 999px;
  background:    linear-gradient(90deg, rgba(59,130,246,0.90), rgba(99,179,237,0.65));
  box-shadow:    0 0 8px rgba(59,130,246,0.28);
  transition:    width 0.6s cubic-bezier(.4,0,.2,1);
}

/* =========================================================
   WARNING BANNER (allocation alert)
========================================================= */
.lcta-warning-banner {
  display:       flex;
  align-items:   flex-start;
  gap:           10px;
  padding:       12px 14px;
  border-radius: 16px;
  background:    rgba(245,158,11,0.10);
  border:        1px solid rgba(245,158,11,0.28);
  box-shadow:    0 0 18px rgba(245,158,11,0.10);
  font-size:     13px;
  font-weight:   600;
  color:         var(--text);
  line-height:   1.5;
}

/* =========================================================
   SCROLLBAR — minimal, iOS-feel
========================================================= */
::-webkit-scrollbar { width: 4px; height: 4px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb {
  background:    rgba(99,120,180,0.22);
  border-radius: 999px;
}
:root[data-theme='dark'] ::-webkit-scrollbar-thumb {
  background: rgba(255,255,255,0.12);
}
